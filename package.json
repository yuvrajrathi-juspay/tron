{
  "name": "lightwake",
  "version": "1.0.0",
  "description": "LIGHTWAKE — a neon light-cycle arena game against three bots. Every mesh, texture and sound is generated in code.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node test/sim-test.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "pngjs": "^7.0.0",
    "puppeteer-core": "^25.12.0",
    "vite": "^8.3.0"
  }
}
